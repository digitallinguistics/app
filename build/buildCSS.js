export default async function buildCSS() {
  // Currently all styles are critical CSS.
  // These styles are inlined during the HTML build step.
  // Later, the buildCSS() will build the CSS for individual pages.
}
