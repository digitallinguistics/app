import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title='Components/Block Header' />

# Block Header

The Block Header is used as the heading/label for larger blocks of content in the app, such as the title of a list component or a section of a page. It should be placed on an `<h1>`–`<h6>` tag.

<Canvas>
  <h1 class='block-header'>Block Header</h1>
</Canvas>
